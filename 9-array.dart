void main() {
  var arr = ['a', 'b', 'c', 'd', 'e'];
  arrayFun(arr);
}

void arrayFun(List arr) {
  print(arr.first); // Mostra primeiro elemento da Array
  print(arr.last); // Mostra o ultimo elemento da Array
  print(arr.isEmpty); // Checa se o array está vazio ou não
  print(arr.length); // Tamanho do Array.
  print(arr[2]); // C
}
